# ---------------------------------------------------------------------------
# CREDENTIALS - never commit. This repository is the execution layer; it holds
# agent definitions, skills, tests, source pointers and tool policy. It holds
# no secrets, no connector code, and no client data.
# ---------------------------------------------------------------------------
client_secret*.json
*credentials*.json
*token*.json
.env
.env.*
!.env.example
*.pem
*.key
*.p12
service-account*.json
.credentials/
secrets/

# Local scratch
node_modules/
.DS_Store
*.log
tmp/
scratch/

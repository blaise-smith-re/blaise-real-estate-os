# Readiness correction — September 10, 2026

**Historical pre-release validation.** The subsequent owner-authorized release is recorded in [RELEASE.md](RELEASE.md). At this correction checkpoint the status was NOT APPROVED FOR RELEASE. Existing `codex/lead-engine-v1` branch, PR #6 and private Drive review package. No merge, governing promotion or Work project installation. The initial demonstration (zero sellers/hosts) remains historical evidence in VALIDATION.md; it did not meet the owner's business-production standard.

## Engineering

43 focused tests and all 40 existing runtime tests pass using direct Node commands (`node --test production/lead-engine/tests/*.test.js` and `node --test tests/runtime/*.test.js`; npm is not on this host's PATH). New coverage includes classes, event participation versus cold contact, unsupported gate bypasses, host evidence, existing full-server restricted read facade, and authentication resume preserving completed work, blocked subqueries, criteria, position and idempotency. The runtime write guard remains unchanged.

Static results remain 31/39 with the same failures previously reproduced on untouched main: T10, T11, T12, T19, T23, T25, T28 and T38. These inherited frontmatter/improvement-log/automation-text issues are not waived. Existing certification is not expanded. Applicable review/merge requirements still apply.

The portable bundle contains this family, the thin skill and only the existing runtime helpers used. Its manifest is compared with independently extracted bytes and tests run from the extraction. The private manifest records exact published hashes and visual checks. No live candidate, authenticated page body, OAuth material or governing body is committed.

## Repeat live research

Blaise completed Northstar authentication; the operator verified actual Matrix search/results. Displayed Single Family criteria were Woodbury/Cottage Grove and June 12–September 10 for Expired/Cancelled. Active used those municipalities without an invented date filter. The private checkpoint retains this bounded run.

- Seller: four expired resale records and three canceled records received detail/history review. Two cases surfaced: one active-to-expired record and one recent cancellation whose current representation remains unresolved. Active relists were excluded; an expiration after a long TNAFS period fell below the unchanged 55 floor. No owner identities were guessed or retained from incidental public records.
- Hosting: eleven rows screened from 429 active matches, with three detailed inspections. Two listing-agent proposals surfaced using price/history/market-position evidence, including a same-day price reduction. No host invitation, need, buyer traffic, slot or agent permission is claimed. Disabled/empty open-house UI is only a scoped observation; unpublished coverage remains unknown.
- Professional: official builder/project/event sources were reopened. One builder relationship surfaces as PIPELINE with an upcoming business context. A local networking event is a lower-ranked alternative whose participation example separates registration, guest eligibility, availability and spending from cold contact.

The five-card board has **4 MONEY NOW, 1 PIPELINE, 0 NETWORK / KNOWLEDGE**. MONEY NOW means plausible near-term business worth evaluating, not approved outreach or an acquired client. Scores remain explicit; service readiness does not inflate the sales count. All outbound contact remains held.

Withdrawn was not exposed in the inspected Matrix form; TNAFS was not relabeled. Repeat public FSBO discovery did not establish a current permitted qualifying owner listing. Stale portal results and restrictive collection terms were not bypassed. Those subqueries remain limited while supported work is retained.

## FUB live diagnosis

The initial task exposed no FUB tools. The clone's read-only config omitted exact discovery, and the clone was not trusted. The existing trusted workspace's full connection reported Not logged in. The new facade reuses exact shortlist discovery through that existing connection without exposing writers.

Blaise completed Auth0 login in the Google profile associated with the connection. A read-only callback succeeded, but real MCP initialization then returned `insufficient_scope`, explicitly requiring `fub:write`. The existing full server requires its configured read/write scopes even to initialize. Scope permission never authorizes an engine write. Callback/resource/issuer/PKCE settings were checked; failed/expired URLs were not reused; app/configuration were preserved.

At this validation checkpoint, the configured-scope flow still required a successful callback and actual exact lookup. The private review manifest records the final live outcome. Do not infer a CRM read from authentication or a fictional adapter test. See [FUB-READ-RECOVERY.md](FUB-READ-RECOVERY.md).

## Integration for final Work review — September 10

After Work approval, MCP PR #5 and OS PR #7 were merged in that order. PR #6 incorporates OS main `6d186ed948696994dcb7cb0166fac1b38b447b47`; only overlapping changelog, decision and improvement entries needed conflict resolution, with both sides retained. No Lead Engine executable behavior, ranking defaults, contact controls or runtime adapter changed.

One fresh Codex 0.154.0 process from the trusted OS workspace initialized the full server authenticated with all 38 tools. The completed exact shortlist read from infrastructure acceptance is reconciled as **zero bounded matches**, with the name-search nonexistence caveat. No new FUB query or OAuth test was needed. The original Matrix checkpoint, observed times, candidate order, scores and class counts are preserved; a dated private addendum and updated selected prep distinguish the completed relationship check from source freshness and outreach readiness.

The original FUB diagnosis above is historical. Durable authentication is accepted; the old Google 401 cause is OPEN / UNVERIFIED and Google development keys are non-blocking hardening. The existing private Drive package and portable source are updated in place. PR #6 remains open and unmerged for final Work release review.

## Boundary (unchanged)

The correction demonstrates actual property research and a more useful slate; it does not establish repeatable conversion or revenue. Work/owner must review before release. No outreach, FUB write, registration, Calendar action, MLS mutation, monitoring, schedule or spending. Authorized private review filing and repository publication are recorded separately from the engine's zero operational effects. Released production families, source registry, audit and governing bodies remain unchanged. Governing pointers remain proposals.
